# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.cursor
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch / calibration dumps
tmp-*
.env
.env.*
!.env.example

# Generated engine-audit output (JSON dumps, checkpoints, CPU profiles) —
# runs into the multi-GB range, never meant to be committed. Keep the small
# hand-relevant summaries (artifacts/*.md, artifacts/finance-allocation-*.json)
# out of this; only the raw generated data directories/files are ignored.
artifacts/engine-audit/
artifacts/scenarios/
artifacts/disc-flight/
artifacts/recommendations/
*.cpuprofile
engine-audit-*.json
